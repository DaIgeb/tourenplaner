{
  "title": "SeasonConfiguration",
  "type": "object",
  "properties": {
    "id": {
      "type": "number"
    },
    "seasonStart": {
      "type": "string",
      "format": "date-time"
    },
    "seasonEnd": {
      "type": "string",
      "format": "date-time"
    },
    "eveningStart": {
      "type": "string",
      "format": "date-time"
    },
    "eveningEnd": {
      "type": "string",
      "format": "date-time"
    },
    "holidayStart": {
      "type": "string",
      "format": "date-time"
    },
    "holidayEnd": {
      "type": "string",
      "format": "date-time"
    },
    "dates": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "date":  {
            "type": "string",
            "format": "date-time"
          },
          "type":  {
            "type": "object",
            "properties": {
              "id": {
                "type": "number"
              },
              "label": {
                "type": "string"
              }
            }
          },
          "description":  {
            "type": "string"
          }
        }
      }
    },
    "specialDates": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "date-time"
      }
    }
  },
  "required": [
    "seasonStart",
    "seasonEnd",
    "eveningStart",
    "eveningEnd",
    "dates",
    "specialDates"
  ]
}